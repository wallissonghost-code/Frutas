name: QA rapido + visual - Frutas

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quick-qa:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Node 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Instalar dependencias
        run: npm install
      - name: Instalar Chromium
        run: npx playwright install --with-deps chromium
      - name: Rodar QA funcional rapido
        run: npm run test:quick
      - name: Rodar QA visual mobile
        run: npm run test:visual
      - name: Guardar relatorio e screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: frutas-qa-quick-visual-report
          path: |
            playwright-report/
            test-results/
          if-no-files-found: ignore
